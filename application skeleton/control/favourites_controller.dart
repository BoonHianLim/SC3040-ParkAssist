// This class implements the FavouritesController
import '../entity/carpark.dart';

class FavouritesController {
  // get list of favourite carparks
  List<CarPark> fetchFavouritesList() {
    return [];
  }

  // add a carpark to list of favourites, then return the updated list of favourite carparks
  List<CarPark> addToFavourites(CarPark carpark) {
    return [];
  }

  // remove a carpark from list of favourites, then return the updated list of favourite carparks
  List<CarPark> removeFromFavourites(CarPark carpark) {
    return [];
  }
}
