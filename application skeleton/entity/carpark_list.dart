import 'carpark.dart';

// This class implements the CarparkList entity with
// the attribute carparks
class CarParkList {
  // list of all carparks
  List<CarPark>? carparks;
  // constructor
  CarParkList({this.carparks});
}
