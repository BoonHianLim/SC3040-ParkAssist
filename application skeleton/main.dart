import 'package:flutter/material.dart';

//main file that runs app
void main() {
  runApp(const MyApp());
}

//class MyApp runs app
class MyApp extends StatelessWidget {
  const MyApp({super.key});

  //render initial widget
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
